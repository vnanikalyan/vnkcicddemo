version: 0.3
          
phases:
  install:
    commands: 
  pre_build:
    commands:      
  build:
    commands:
      - node main.js
  post_build:
    commands:
      - echo Build completed on `date`